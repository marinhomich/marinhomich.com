{
  "name": "marinhomich",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev --parallel",
    "clean": "turbo clean && rimraf node_modules .turbo out",
    "lint": "turbo lint"
  },
  "devDependencies": {
    "rimraf": "^5.0.5"
  },
  "packageManager": "pnpm@8.9.0",
  "engines": {
    "node": ">=18"
  }
}
